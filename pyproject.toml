[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "embedded-board-registry"
version = "0.2.3"
description = "Offline, fail-closed board identity resolution for embedded agents"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Nitmi" }]
keywords = ["embedded", "hardware", "identity", "agent", "cli"]
dependencies = []

[project.urls]
Homepage = "https://github.com/Nitmi/board-registry"
Repository = "https://github.com/Nitmi/board-registry"
Issues = "https://github.com/Nitmi/board-registry/issues"

[project.optional-dependencies]
dev = ["jsonschema>=4.23", "pytest>=8.3", "ruff>=0.11"]
binary = [
  "pyinstaller==6.22.2",
  "tomli>=2.0; python_version < '3.11'",
]

[project.scripts]
board-registry = "board_registry.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/board_registry"]

[tool.hatch.build.targets.wheel.force-include]
"schemas" = "board_registry/schemas"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
